# ── NOTE on replication factors ──────────────────────────────
# The RF=3 values below are the production target. In local
# docker-compose (single broker), they are overridden to RF=1
# via the CONNECT_*_REPLICATION_FACTOR environment variables.
# ─────────────────────────────────────────────────────────────

group.id=debezium-connect-cluster
bootstrap.servers=kafka:29092

config.storage.topic=debezium-connect-configs
config.storage.replication.factor=3

offset.storage.topic=debezium-connect-offsets
offset.storage.replication.factor=3
offset.storage.partitions=25

status.storage.topic=debezium-connect-status
status.storage.replication.factor=3
status.storage.partitions=5

key.converter=io.confluent.connect.avro.AvroConverter
key.converter.schema.registry.url=http://schema-registry:8081
value.converter=io.confluent.connect.avro.AvroConverter
value.converter.schema.registry.url=http://schema-registry:8081

errors.tolerance=all
errors.log.enable=true
errors.log.include.messages=true
errors.retry.timeout=300000
errors.retry.delay.max.ms=10000

plugin.path=/usr/share/java,/usr/share/confluent-hub-components

rest.port=8083
rest.advertised.host.name=kafka-connect
rest.advertised.port=8083

offset.flush.interval.ms=10000
offset.flush.timeout.ms=5000
task.shutdown.graceful.timeout.ms=30000

producer.compression.type=snappy
producer.max.request.size=1048576
producer.linger.ms=50
producer.batch.size=16384

consumer.max.poll.records=500
consumer.max.poll.interval.ms=300000